# Minimal requirements for Fast Style Transfer node
# These won't conflict with existing packages
Pillow>=8.0.0          # For image processing
numpy>=1.19.0          # Compatible with existing packages

# Optional dependencies: 
# tensorflow and tensorflow-hub are used in an isolated subprocess
# so specific versions don't need to be installed in the main environment